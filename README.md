# A NODEJS server to query the Zoom REST API 2.0 (Proof of concept)

# How to run instructions
1. ```npm install```
2. In config.js change the JWT token to your Apps JWT Token from Zoom. 
3. ```npm run serve```

The server will start on localhost:1337